# Java
add() 方法与 offer() 方法类似，它们都是用来向队列中添加元素的。两者的区别在于当队列的容量达到了最大值时，add() 方法会抛出一个 IllegalStateException 异常，
而 offer() 方法只会返回 false。通常情况下，推荐使用 offer() 方法来添加元素，因为它能够更好地处理队列满的情况。而 add() 方法更适合用于在保证队列未满的情况下添加元素。
offer() 是 Java Deque 接口提供的方法，用于将元素插入队列的末尾。这个方法有两种实现：

offer(E e)：将指定元素插入队列末尾（如果队列未满则成功，如果队列已满则返回 false）
offerFirst(E e)：将指定元素插入队列头部（如果队列未满则成功，如果队列已满则返回 false）

eg：
  这里的 offer(new int[] {f(si,sj),f(bi,bj),0}) 就是将一个整型数组插入队列末尾，
  数组的元素是 (f(si, sj), f(bi, bj), 0)，即当前人和箱子的位置以及当前步数


